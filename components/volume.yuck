
(defwidget volume_component []
 (metric :icon `🔊`
   :label `${volume}%`
   :value volume
   :labelClass 'music-label'
   :valign 'senter'
   :spacing 10
      :h "h"
      :iconClass "volumeIcon"
            :onchange "amixer -D pulse sset Master {}%")

  )


(defpoll volume :interval "1s"
  "amixer -D pulse sget Master | grep 'Left:' | awk -F'[][]' '{ print $2 }' | tr -d '%' | head -1")
